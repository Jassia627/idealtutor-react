import React from "react";
import "./Spinner.css"; // Asegúrate de que este archivo contenga los estilos para el spinner.

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="spinner-circle"></div>
    </div>
  );
};

export default Spinner;
